# Debugging Row Indices
print("First few row indices in valid set:")
print(head(valid_indices))  

print("First few row indices in predictions:")
print(head(mod_prob_auto_indemnity$pred$rowIndex))

# Check overlap
print("Common row indices between valid_indices and rowIndex:")
print(intersect(valid_indices, mod_prob_auto_indemnity$pred$rowIndex))