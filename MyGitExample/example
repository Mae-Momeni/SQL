# Join data_train with predictions on rownum and rowIndex
alignment_check <- data_train %>%
  select(rownum) %>%
  inner_join(mod_prob_auto_indemnity$pred, by = c("rownum" = "rowIndex"))

# Check the number of matched rows
print(nrow(alignment_check))

# Compare to total rows in data_train
print(nrow(data_train))