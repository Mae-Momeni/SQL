print("First few rownum values in data_train:")
print(head(data_train$rownum))

print("First few rowIndex values in predictions:")
print(head(mod_prob_auto_indemnity$pred$rowIndex))

# Check for any overlap
print("Intersection of rownum and rowIndex:")
print(intersect(data_train$rownum, mod_prob_auto_indemnity$pred$rowIndex))