# Load the YAML file
config <- yaml::read_yaml("config/config.yaml")

# Access modeling-related configurations
modeling_config <- config$modeling

# Access paths for models
v2_path <- config$model_paths$v2_path
selected_path <- config$model_paths$selected_path